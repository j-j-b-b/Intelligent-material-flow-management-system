tags:  # 接口名称
  - 数据库接口
summary: 输入接口  # 接口描述
post:
  parameters:  # 参数
    - name: address_x  # 参数名
      in: formData  # 参数类型
      type: string  # 参数值的类型
      description: 地址_x  # 参数描述
      required: true  #必填项
    - name: address_y  # 参数名
      in: formData  # 参数类型
      type: string  # 参数值的类型
      description: 地址_y  # 参数描述
      required: true  #必填项
    - name: number  # 参数名
      in: formData  # 参数类型
      type: string  # 参数值的类型
      description: 电话  # 参数描述
      required: true  #必填项
    - name: name  # 参数名
      in: formData  # 参数类型
      type: string  # 参数值的类型
      description: 姓名  # 参数描述
      required: true  #必填项

responses:
  200:
    description: 处理了登录请求
    schema:
      type: string
      default: 成功
